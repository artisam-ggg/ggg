# dependencies
node_modules/
.pnpm-store/

# next
.next/
out/
next-env.d.ts

# env
.env
.env.local
.env.*.local

# build / artifacts
dist/
build/
*.tsbuildinfo

# rust
target/
test_snapshots/

# prisma
apps/web/src/generated/

# os / editor
.DS_Store
*.log
coverage/
.claude/

# local-only docker override (shared-sandbox port remap)
docker-compose.override.yml

# Claude Code local worktrees (never commit)
.claude/worktrees/

# Playwright E2E artifacts + funded fixture keypairs (Testnet secrets, never commit)
apps/web/.e2e/
apps/web/playwright-report/
apps/web/test-results/

# Local verification artifacts (disposable)
.env.verify
.verify-compose.yml

# Local / scratch directories and files (never commit)
engr
engr/

# SCF scoping documents — generated locally, not part of the shipped codebase
docs/scf-30d-scope.md
