{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter web db:generate && pnpm --filter web build"
  },
  "deploy": {
    "startCommand": "pnpm --filter web start",
    "preDeployCommand": "pnpm --filter web exec prisma migrate deploy && pnpm --filter web exec prisma db seed",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3,
    "healthcheckPath": "/health"
  }
}
