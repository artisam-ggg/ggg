# D1 — Deadline-enforced escrow

## SOW commitment

Add a settlement deadline and a permissionless refund path for a tournament that has neither been finalized nor cancelled. The SOW names the intended function `claim_refund_after_deadline()` and also requires failure-path testing before the deadline.

## Implemented contract behavior

The current escrow ABI implements the deadline-refund outcome through `claim_refund(player)`, rather than a literal `claim_refund_after_deadline()` entrypoint. The contract stores `settlement_deadline` during construction, rejects a deadline that is not in the future or exceeds the configured maximum horizon, and exposes the deadline through `get_tournament()` and `get_settlement_deadline()`.

For an active tournament, `claim_refund` allows the registered player to be paid after the inclusive deadline; cancellation enables immediate individual claims. The contract source comments and regression matrix describe the call as permissionless for the caller while payment remains directed to the registered player. This is an implementation/behavior mapping to the SOW goal; it is not a claim of literal method-name equality.

## Traceability

| SOW criterion | Implementation/evidence source | Status rule |
| --- | --- | --- |
| Deadline stored at construction | [Escrow source](https://github.com/artisam-ggg/ggg/blob/develop/contracts/escrow/src/lib.rs) | Source-level implementation evidence. |
| Refund succeeds after the deadline | [Issue #218 evidence](https://github.com/artisam-ggg/ggg/blob/develop/docs/verification/issue-218-testnet-evidence.md) | Mark Testnet verified only when an explorer transaction proves the post-deadline refund. |
| Refund fails before the deadline | [Regression evidence](https://github.com/artisam-ggg/ggg/blob/develop/docs/verification/issue-221-regression-evidence.md) | Passing contract test evidence supports local verification. |
| Authorization, cancellation, and terminal-state races | [Regression evidence](https://github.com/artisam-ggg/ggg/blob/develop/docs/verification/issue-221-regression-evidence.md) | The regression matrix lists deadline boundary, caller/recipient, cancellation, and refund-after-finalization cases. |

## Evidence gap check

The final reviewer package should identify the specific Testnet refund transaction and contract instance. Do not substitute a build, a WASM upload, or a unit-test result for that live acceptance evidence.

## Current evidence boundary

The #218 record confirms a constructor-WASM upload and verification but expressly says that the deployment test and live join were not run. It cannot prove D1's required live after-deadline refund. Before this deliverable can be marked Completed, record the deployed contract ID, the post-deadline refund transaction, the pre-deadline failure evidence, and an immutable test or CI result.
